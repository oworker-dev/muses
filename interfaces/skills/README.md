# Consumer Agent Skills

Describe runtime skills that consumer agents can use to call this service.

Do not confuse this with `.agents/skills/`, which is for developer agents maintaining the project.
