# Image To Editable Interface

Status: proposed, not callable, and intentionally absent from OpenAPI, ANSS,
CLI, MCP, and runtime capability discovery.

The draft HTTP and manifest contracts are documented in
`docs/internal/image-to-editable/技术方案.md`. This interface directory becomes
authoritative only after architecture decision `ai-3` and the contract gate are
approved.
