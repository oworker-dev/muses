# Glossary

- Developer Agent: an agent that helps maintain this codebase.
- Consumer Agent: an agent that discovers and calls this service at runtime.
- Interface surface: files under `interfaces/` that describe externally callable capabilities.
- Implementation root: files under `src/` that implement behavior.
- Provider: a replaceable adapter for an external service or infrastructure dependency.
