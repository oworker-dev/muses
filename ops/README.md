# Ops

Runtime, deployment, health, and observability notes live here.

Use `ops/docker/` for container and Docker Compose assets.

Workflow SDK database isolation, read-only diagnosis, and recoverable cleanup
are documented in [Workflow World Isolation And Recovery](workflow-world-recovery.md).
