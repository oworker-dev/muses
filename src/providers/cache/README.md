# Valkey Cache Provider

The default cache provider is Valkey through Docker Compose.

Use `REDIS_URL` to connect application code to the cache runtime. Keep direct client usage inside provider adapters.
