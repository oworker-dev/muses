# Database Provider

The default SaaS Starter database provider is PostgreSQL.

Docker Compose uses `pgvector/pgvector:pg17`, which keeps future vector capabilities available without making AI mandatory.
