# MCP App

This app is the canonical Agent API surface.

It is a runnable skeleton so developers can add real MCP tools without inventing a second service layout. Tool contracts must be mirrored in `interfaces/mcp` and contract tests.

The default skeleton lists health, integration, account, billing, and presigned upload contracts without implementing product-specific Agent workflows.
