# Worker App

This app is the background execution surface for async jobs.

The default local runtime includes Valkey and a BullMQ worker surface. Add project-specific processors only after a concrete job contract exists.
