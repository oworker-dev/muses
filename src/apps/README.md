# Apps

Runnable application entrypoints live here.

- `web/` is the default runnable app.
- `miniapp/`, `mobile/`, and `desktop/` are reserved for derived starters.
- `media-api/` and `media-worker/` are proposal-only composition roots for the
  host-neutral AI media execution boundary. They are not runnable until the
  pending architecture decision is approved.
