# SDK

Client SDKs for humans, agents, and services live here.

SDK packages contain public contracts, clients, authentication hooks, and
transport helpers. They must not contain server orchestration, provider
credentials, model SDKs, queue consumers, or image-processing implementations.

The proposed host-neutral media client boundary is documented under `media/`.
